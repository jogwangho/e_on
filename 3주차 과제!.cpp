#include <iostream>
using namespace std;



int main(void) {
	int pvalue[6] = { 0 };
	int i;

	for (i = 0; i < 6; pvlaue[i]) {
		cout << i << "번째 숫자를 입력하시오." << endl;
		cin >> ivalue[i];
		mysum(pvalue);
	}
}

int mysum(pvalue) {
	int isum = 0;
	for (i = 0; i < 6; pvalue) {
		isum += pvalue;
	}
	cout << "다음 숫자들의 합은" << isum << "입니다" << endl;
}